# Dependências
node_modules/
backend/node_modules/

# Variáveis de ambiente (NUNCA versionar — contém segredos)
.env
.env.local
backend/.env
backend/.env.local

# Dados pessoais de clientes e logs de acesso — NUNCA versionar
# (nome, telefone e mensagens de contato; IPs de visualização).
# produtos.json continua versionado de propósito: é catálogo público,
# sem dado pessoal.
backend/data/contatos.json
backend/data/visualizacoes.json

# Logs
*.log
npm-debug.log*

# Sistema
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/

# Build/cache
.cache/
dist/
